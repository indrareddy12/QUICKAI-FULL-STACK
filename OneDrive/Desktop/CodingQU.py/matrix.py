# Define the element, number of columns, and number of rows
o = 0
cols = 4
rows = 3

# Create the 2D list
matrix = [[o] * cols for _ in range(rows)]

# Print the matrix
for row in matrix:
    print(row)
